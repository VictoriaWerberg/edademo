# Netlify configuration
# netlify.toml lives at the repo root; publish dir points into site/

[build]
  publish = "site"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
